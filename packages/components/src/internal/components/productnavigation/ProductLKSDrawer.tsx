import React, { FC, memo } from 'react';

interface ProductLKSDrawerProps {

}

export const ProductLKSDrawer: FC<ProductLKSDrawerProps> = memo(props => {
    // const { product, projects } = props;

    return (
        <>
            Options for LabKey Server
        </>
    );
});
